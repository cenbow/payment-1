spring:
  application:
    name: ms-payment-notification
  cloud:
    config:
      discovery:
        enabled: true
    zookeeper:
      discovery:
        register: true
  main:
    allow-bean-definition-overriding: true