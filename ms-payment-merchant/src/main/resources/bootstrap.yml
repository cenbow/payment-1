spring:
  application:
    name: ms-payment-merchant
  cloud:
    config:
      discovery:
        enabled: true
    zookeeper:
      discovery:
        register: true
  main:
    allow-bean-definition-overriding: true